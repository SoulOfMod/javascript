//normal

roundedNumber = 3.6;

var total = roundedNumber - Math.floor(roundedNumber);

if (total >= 0.5) {

    console.log(Math.ceil(roundedNumber))
} else {
    console.log(Math.floor(roundedNumber))
}

//string (voir corrigé notion)
